import './App.css';
import BookList from './BookList';

// Root component — renders the BookList component for the bookstore app
function App() {
    return (
        <>
            <BookList />
        </>
    );
}

export default App;
